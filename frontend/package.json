{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.21.4",
    "mobx": "^6.3.3",
    "mobx-react-lite": "^3.2.1",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "scripts": {
    "start": "sst-env -- npm run start-ui",
    "start-ui": "npx parcel src/index.html --open --port=3000",
    "clean": "rimraf ./build",
    "build": "npm run clean && npx parcel build src/index.html --out-dir build",
    "test": "echo test is not implemented yet"
  },
  "devDependencies": {
    "@serverless-stack/static-site-env": "^0.43.3",
    "@types/node": "^12.20.26",
    "@types/react": "^17.0.24",
    "@types/react-dom": "^17.0.9",
    "parcel-bundler": "^1.12.5",
    "rimraf": "^3.0.2",
    "typescript": "4.4.3"
  }
}
